{
    "build": {
      "builder": "NIXPACKS",
      "config": {
        "installCommand": "pip install -r requirements.txt && yarn install",
        "startCommand": "python manage.py migrate && gunicorn doan_diemdanh_new.wsgi --bind 0.0.0.0:$PORT & cd frontend && yarn start",
        "baseImage": "ubuntu-22.04"
      }
    }
  }