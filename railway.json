{
    "build": {
      "builder": "NIXPACKS",
      "config": {
        "installCommand": "pip install -r requirements.txt",
        "startCommand": "python manage.py migrate && gunicorn doan_diemdanh_new.wsgi"
      }
    },
    "deploy": {
      "startTimeout": 180
    }
  }