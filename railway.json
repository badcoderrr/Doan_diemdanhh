{
    "build": {
      "builder": "NIXPACKS",
      "config": {
        "installCommand": "pip install gunicorn==21.2.0 && pip install -r requirements.txt",
        "startCommand": "gunicorn doan_diemdanh_new.wsgi --bind 0.0.0.0:8000 --workers 3"
      }
    }
  }